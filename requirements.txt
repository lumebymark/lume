# Root requirements.txt — Nixpacks reads this during build
# (backend/requirements.txt is for local dev reference only)

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
supabase>=2.0.0

# Authentication
python-jose[cryptography]>=3.3.0

# Data validation
pydantic>=2.0.0

# HTTP client
httpx>=0.26.0

# Environment variables
python-dotenv>=1.0.0

# Image optimisation (journal uploads -> resized WebP)
Pillow>=10.3.0

# AI editing
python-multipart>=0.0.9
anthropic>=0.40.0
pdfplumber>=0.11.0
python-docx>=1.1.2
